- name: play level variables
  hosts: local
  connection: local
  vars: 
    COURSE: Ansible
    DURATION: 10HRS
    Trainer: Shankar
  tasks:
  - name: print the course info
    ansible.builtin.debug:
      msg: "course name : {{ COURSE }} {{ DURATION }} {{ Trainer }}"