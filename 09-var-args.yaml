#before this you delete the declared variables from inventory file
- name: variables from arguments
  hosts: local
  connection: local
  tasks:
  - name: print the course info through args
    ansible.builtin.debug:
      msg: "course : {{ COURSE }},
            duration : {{ DURATION }},
            trainer : {{ TRAINER }}"