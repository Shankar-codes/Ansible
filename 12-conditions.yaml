- name: conditions
  hosts: local
  connection: local

  vars:
    number: 20
  
  tasks:
    - name: check the number less than 20 or not
      ansible.builtin.debug:
        msg: "the number is {{ number }} is less than 20"
  
      when: number < 25